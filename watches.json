{
  "watches": [
    {
      "type": "checks",
      "state": "passing",
      "handler_type": "http",
      "http_handler_config": {
        "path": "http://localhost:4000/watch",
        "method": "POST",
        "header": {
          "x-foo": ["bar", "baz"]
        },
        "timeout": "10s",
        "tls_skip_verify": true
      }
    }
  ]
}
